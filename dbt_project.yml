name: 'ecommerce_data_platform'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_data_platform'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ecommerce_data_platform:
    +materialized: table